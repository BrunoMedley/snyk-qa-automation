# Copy this file to .env.dev and set real values (never commit .env.dev).
#   cp env/.env.dev.example env/.env.dev

BASE_URL=http://localhost:8080 #change to the base url of the application
USER_EMAIL=your-test-user@example.com 
USER_PASSWORD=your-test-password
TIMEOUT=3000
